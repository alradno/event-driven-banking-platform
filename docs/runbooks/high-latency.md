# Runbook: High Latency

1. Compare gateway latency with downstream service latency.
2. Check database connection pools and slow queries.
3. Inspect Kafka broker health if event publishing latency rises.
4. Use trace IDs to isolate the slow span.
5. Verify whether simulations are enabled.
